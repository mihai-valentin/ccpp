{
  "name": "ccpp",
  "version": "0.1.0",
  "description": "Private Claude Code skill and slash-command distribution from any git host — short names, live reload, lockfile-pinned.",
  "license": "MIT",
  "author": "Mihai Valentin",
  "type": "module",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "bin": {
    "ccpp": "dist/cli.cjs"
  },
  "files": ["dist", "LICENSE", "CHANGELOG.md", "README.md"],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "cac": "^6.7.14"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.14.0",
    "tsup": "^8.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
