node_modules/
dist/
*.tgz
*.log
.env
.env.*
!.env.example
*.tsbuildinfo
coverage/
.vitest-cache/
.cache/

# ccpp-produced runtime artifacts. This repo is the PRODUCER, not a consumer
# of itself — if a contributor dogfoods ccpp from here, these land in cwd
# and should not be tracked.
.ccpp/
ccpp.lock
ccpp.lock.json
ccpp.config.json

# Root-session-owned artifacts (managed by xlnf root, not by ccpp's own git)
CLAUDE.md
idea.md
.claude/

# Shawarma orchestration state (per-project DB, runs, task backups)
.agent-orch/

# Shawarma worktrees scratch area
.worktrees/
